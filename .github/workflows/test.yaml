name: Build Project with epiclang

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run epiclang
        uses: epitechcontent/epitest-docker@v1
        with:
          timeout-minutes: 2
          run: make